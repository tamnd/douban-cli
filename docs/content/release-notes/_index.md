---
title: "Release notes"
linkTitle: "Release notes"
description: "What changed in each douban release, newest first."
weight: 40
---

What shipped in each release, newest first. Every tagged version builds the same
set of artifacts: archives for Linux, macOS, Windows, and FreeBSD, Linux
packages (deb, rpm, apk), a multi-arch container image on GHCR, and entries for
the package managers. Binaries are pure Go, so there is nothing to install
alongside them.

- [v0.3.0](v0.3.0/) - one registry, three surfaces: CLI, HTTP API, and MCP server.
- [v0.2.0](v0.2.0/) - the offline mirror: crawl Douban into a local store.
- [v0.1.0](v0.1.0/) - the first release: read-only lookup commands.
